# Copy to .env and fill in. Only the provider you actually use is required.
# Default generator + judge is gpt-4o-mini, so OPENAI_API_KEY is needed out of the box.
OPENAI_API_KEY=sk-...
# Optional: only if you switch generator/judge provider to anthropic in config/rag.yaml
ANTHROPIC_API_KEY=sk-ant-...
