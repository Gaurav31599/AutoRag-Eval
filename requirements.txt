# Grows one phase at a time to keep installs fast and avoid premature
# dependency conflicts.

# P0 — config-driven RAG target
pyyaml>=6.0
python-dotenv>=1.0
datasets>=2.19
sentence-transformers>=3.0
chromadb>=0.5
openai>=1.30
anthropic>=0.34
tiktoken>=0.7

# P1 — eval harness (RAGAS + LLM-as-judge) and dashboard.
# Pinned: ragas 0.2.x is stable on the langchain 0.3.x line. langchain 1.x
# broke module paths ragas imports, so we hold the whole langchain stack at 0.3.
ragas==0.2.14
langchain==0.3.27
langchain-core==0.3.79
langchain-community==0.3.31
langchain-openai==0.2.14
langchain-anthropic==0.2.4
langchain-huggingface==0.1.2
streamlit>=1.38
plotly>=5.23
pandas>=2.0
