# P0 — config-driven RAG target. Grows one phase at a time to keep
# installs fast and avoid premature dependency conflicts (ragas/deepeval
# land in P1/P2).
pyyaml>=6.0
python-dotenv>=1.0
datasets>=2.19
sentence-transformers>=3.0
chromadb>=0.5
openai>=1.30
anthropic>=0.34
tiktoken>=0.7
